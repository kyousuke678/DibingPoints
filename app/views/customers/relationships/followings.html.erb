<div class='container'>
  <div class='row'>
    <h2>Follow Users</h2>
    <% if @users.exists? %>
      <%= render 'customers/users/index', users: @users %>
    <% else %>
      <p>ユーザーはいません</p>
    <% end %>
  </div>
</div>